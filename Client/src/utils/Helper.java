package utils;

public class Helper {

    /*public static int intFromString(String s, int nbBytes){
        int[] bytes = new int[nbBytes];
        for(int i = 0; i < nbBytes; i++){
            bytes[i] = s.charAt(i);
        }
        ByteBuffer buffer = ByteBuffer.allocate(bytes.length*Integer.BYTES);


    }*/
}
